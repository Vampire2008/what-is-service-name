name: Testing

on:
    workflow_call:

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v7

            - name: Set up pnpm
              uses: pnpm/setup@v2
              with:
                  runtime: node@latest

            - name: Run tests
              run: pnpm test:unit
